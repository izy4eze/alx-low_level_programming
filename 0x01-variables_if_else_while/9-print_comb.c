#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
* main - Entry point
* This program prints nummber  
* Return: Always 0 (success)
*/

int main(void)
{
	int a;

	for (a = '0'; a <= '9'; a++)
	{
		putchar(d);
		if (a != '9')
		{
			putchar(',');			
		}
	}
	putchar('\n');

	return (0);
}
